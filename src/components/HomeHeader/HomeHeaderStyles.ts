import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingTop: 10,
  },
  userImage: {
    width: 35,
    height: 35,
  },
  userDetails: {
    marginLeft: 10,
  },
});

export default styles;
